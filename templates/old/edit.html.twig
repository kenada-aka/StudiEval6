<!DOCTYPE html>

<html>

    <head>

        <title>{{ title }}</title>

    </head>

    <body>

        <h1>{{ title }}</h1>
        {{ form_start(form) }}

             {{ form_widget(form) }}
        <button>Valider</button>
        {{ form_end(form) }}

    </body>

</html>