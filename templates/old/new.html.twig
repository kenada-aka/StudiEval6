<!DOCTYPE html>

<html>

    <head>

        <title>{{ title }}</title>

    </head>

    <body>

        <h1>{{ title }}</h1>
        {{ form_start(form) }}

             {{ form_rest(form) }}
        <button>Ajouter</button>
        {{ form_end(form) }}

    </body>

</html>