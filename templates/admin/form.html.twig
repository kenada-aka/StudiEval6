
{% extends "index.html.twig" %}

{% block body %}

<main id="main">
    <section class="section">
        <div class="container mt-4">   
            {{ form_start(form) }}
                {{ form_rest(form) }}
                <div class="text-center mt-4"><button type="submit" class="btn btn-primary mt-4">Ajouter</button></div>
            {{ form_end(form) }}
        </div>
    </section>
</main>

{% endblock %}